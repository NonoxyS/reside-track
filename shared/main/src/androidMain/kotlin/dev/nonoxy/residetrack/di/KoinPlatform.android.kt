package dev.nonoxy.residetrack.di

import org.koin.core.module.Module
import org.koin.dsl.module

internal actual fun koinPlatformModules(): List<Module> {
    return listOf(
        module {
        }
    )
}
